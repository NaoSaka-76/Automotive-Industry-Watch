# Automotive Industry Watch

トヨタ自動車の最新トピックス、世界の自動車産業ニュース、モータースポーツ、規制動向を
30分ごとに自動収集し、GitHub Pagesで公開するモニタリングダッシュボード。

## 構成

- **① トヨタ自動車 最新トピックス** — 株価チャート(7203.T、日/週/月/3ヶ月/6ヶ月/1年で切替可能、
  縦横軸ラベル付き)。「比較」でホンダ・日産自動車・スズキ・マツダ・SUBARU・三菱自動車を選択すると、
  期間開始時点を0%とした騰落率での重ね描き比較表示に切り替わる。直近四半期決算(前年同期比)、
  通期業績比較(全年期実績/期初見通し/最新見通しの3列比較)、決算数値を機械的に比較した
  「決算についての考察」(ルールベースの自動生成コメント)を表示。ニュースはトヨタ自動車に関する
  ものを日英で収集し、実際に該当トピックスが存在するカテゴリーのみをタブ化(新製品・新技術・規制・
  経営・政策・他、自動分類・参考値)。英語見出しには簡易日本語訳を併記。最新順/話題順を切替可能。
- **② 自動車産業に関するトピックス** — グローバル・日本・アメリカ・ヨーロッパ・アジア・オセアニア・
  アフリカ・中東・ロシアの8地域+グローバルで収集。各記事は見出しのキーワードから
  新製品・新技術・規制・経営・政策・他へ自動分類(参考値)。地域タブ→カテゴリタブ→最新順/話題順の
  3段階で絞り込み可能。
- **③ モータースポーツ** — F1・WEC/ル・マン(耐久)・WRC/ラリー・NASCAR/IndyCar(北米)・
  Super GT/スーパーフォーミュラ(国内)・Formula E(電動)・クロスカントリーラリー(ダカール等)・
  ドリフト(D1GP等)・全日本ラリー選手権(国内)の9カテゴリーを集約。
- **④ 規制動向** — 日本・アメリカ・ヨーロッパ・中国における、自動車開発に影響する規制動向を
  排気規制・安全性能規制・騒音規制・サイバーセキュリティ規制の4カテゴリーで収集。地域タブ→
  カテゴリタブで絞り込み、各カテゴリーごとに「最新サマリー」(規制動向に関する報道全般)と
  「当局の最新トピックス」(国土交通省・NHTSA・欧州委員会/UNECE・MIIT等、所管当局名を含む報道)を
  並べて表示。

ページ上部の「直近24時間ダイジェスト」で、①トヨタ自動車トピックス/②自動車産業に関するトピックス/
③モータースポーツ/④規制動向の4大分類ごとに24時間以内の更新件数を一覧できる。24時間以内に
公開された記事は一覧上でも赤枠+バッジでハイライトされる。各カテゴリーの記事一覧はスクロール
ウィンドウ形式にしており、ページ全体が際限なく長くならないようにしている。

さらに①〜④の各セクション冒頭に「ダイジェスト」ブロックを表示する。見出しに出現するキーワード
(カタカナ語・漢字語・英単語)を、新しさ(24時間以内を強く重視、以降72時間→1週間で減衰)と
記事本数、および話題順での上位掲載を加味して重み付けし、複数記事で共通して言及されている
注目トピックスを最大3件、代表見出しとあわせて自動抽出する。媒体名・URL断片・汎用語は
ストップワードで除外している(見出しキーワードの機械集計のため参考値)。

## 既知の制約

- **APIキー不使用**: YouTube Data API・X API等の有料/要承認APIは使わず、Google News RSS
  (無料・無認証の公開フィード)のみでデータを収集している。
- **記事本文は取得不可**: Google News RSSのリンクはリダイレクト経由でJS描画される
  `news.google.com` の中間ページを経由するため、`requests` では実際の記事本文を取得できない。
  そのため見出し(タイトル)のみを対象としている。
- **「話題順」は関連度順の代替指標**: 記事本文もエンゲージメント数(閲覧数・シェア数等)も
  取得できないため、「話題順」はGoogle News検索結果内での表示順(関連度順)を代替指標として
  用いている。実際の話題性・拡散度を示す指標ではない。
- **自動分類は参考値**: 自動車産業トピックスのカテゴリー分類(新製品/新技術/規制/経営/政策/他)は、
  見出しテキストのキーワードマッチによる自動推定であり、厳密な分類ではない。
- **地域別クエリの言語**: 日本のみ日本語クエリを使用。それ以外の地域(アメリカ・ヨーロッパ・
  アジア・オセアニア・アフリカ・中東・ロシア)は、すべて英語で報じられた国際ニュースのみを
  対象としている(現地語での報道は対象外)。
- **真のリアルタイム更新ではない**: GitHub Pagesは静的ホスティングのためサーバー側処理ができず、
  GitHub Actionsのcronによる30分毎のバッチ更新となっている。
- **株価データ**: Yahoo Financeの非公式・無認証のチャートAPI(`v8/finance/chart`)から取得している。
  「日」「週」は直近5営業日の15分足、「月」以降は直近1年の日足を取得し、フロントエンド側で
  期間分だけ切り出して表示している。APIキー不要だが非公式エンドポイントのため、構造変化や
  レート制限で取得できなくなる場合がある。
- **決算データ**: トヨタ自動車公式サイトの投資家情報ページ(https://global.toyota/jp/ir/financial-results/
  および archives/03.html)から、直近四半期の決算要旨PDFと直近の通期決算要旨PDF(通期実績+翌期の
  期初見通しを含む)を特定し、PDF本文をテキスト抽出して連結業績の主要数値を取得している。公式一次
  情報だが、IFRS基準の決算短信のため日本基準特有の「経常利益」は存在せず、代わりに「税引前利益」を
  掲載している。「通期業績比較」の期初見通しは直近の通期決算発表時点、最新見通しは直近四半期決算
  発表時点のものであり、両者とも会社側の予想値(実績ではない)。PDFのレイアウトが将来変更された
  場合は抽出できなくなり、該当ダイジェストが空表示になる可能性がある。
- **英語見出しの日本語訳**: ダッシュボード全体(トヨタ自動車トピックス・自動車産業トピックス・
  モータースポーツ・規制動向)の英語見出しすべてに対して、MyMemory Translation API(無料・無認証)で
  簡易的な日本語訳を見出し下に併記している。ただし無料枠には1日あたりの文字数上限があり(匿名利用で
  5000文字/日程度)、ダッシュボード全体では英語見出しが数百件規模になるため、1回の更新の途中で
  枠を使い切ることがある。以前は①→②→③→④の順に個別翻訳していたため後回しのセクション
  (特に④規制動向)が枠切れで毎回ゼロ件になっていたが、現在はfetch_data.py側で全セクションの
  候補(各セクション内は新着順)をラウンドロビンで混ぜてから翻訳するため、枠を使い切って途中で
  打ち切られても特定のセクションだけが割を食うことはない。連続8回失敗すると無料枠切れとみなして
  即座に打ち切る。機械翻訳のため精度も参考程度。より多くの件数を安定して翻訳したい場合は、
  MyMemoryにメールアドレスを渡すことで無料枠を50000文字/日まで拡張できるため、必要であれば
  `scripts/sources/translate.py`にメールアドレスを追加することを検討のこと。
- **株価比較(他メーカー)**: ホンダ(7267.T)・日産自動車(7201.T)・スズキ(7269.T)・マツダ(7261.T)・
  SUBARU(7270.T)・三菱自動車(7211.T)の株価もYahoo Financeの同エンドポイントから取得している。
  各社で株価の絶対水準が大きく異なるため、比較表示では各系列を期間開始時点=0%とした騰落率に
  正規化して重ね描きしている(トヨタ単体表示時のみ実際の株価(円)を表示)。
- **決算についての考察**: LLMや外部の分析サービスは使わず、取得済みの決算数値どうしを比較する
  ルールベースの短文生成に留めている(例: 営業利益と税引前利益の乖離から営業外要因の影響を推測、
  期初見通しと最新見通しの差分から上方/下方修正を判定、四半期実績を通期見通しの単純な1/4と比較)。
  正式な経営分析・投資判断の根拠ではなく、あくまで数値の機械的な比較による参考コメント。

## ローカルでの実行

```bash
cd scripts
python3 -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt
python fetch_data.py
```

`site/data/latest.json` が生成される。`site/` ディレクトリを任意の静的サーバーで配信すれば
ローカルで表示を確認できる(例: `python3 -m http.server --directory ../site 8000`)。

## デプロイ

GitHub Actions (`.github/workflows/update-dashboard.yml`) が30分毎に `scripts/fetch_data.py` を
実行し、`site/` を GitHub Pages にデプロイする。
